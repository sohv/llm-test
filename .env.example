# copy to .env and fill in real values. .env is gitignored.
# this is the only file in the repo that may show a key, and only as a placeholder.

# every model in this study is routed through openrouter, so this is the only key the harness needs.
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxx

WANDB_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxx
